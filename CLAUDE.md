# CLAUDE.md — トウシナビ

**⚠️ 作業前に共通正本を必ず読むこと:**
`/Users/hiroshi/Documents/Obsidian Vault/Projects/アフィリエイト/AFFILIATE.md`

技術スタック・デプロイ手順・禁止事項・ハマりどころは全て共通正本にある。このファイルには**トウシナビ固有の情報だけ**を書く。シカクナビのクローンなので、迷ったら `shikaku-affiliate-blog` の実装を見る。

---

## 🔴 このサイトの最重要ルール — YMYL 対応

**投資助言をしてはいけない。** このサイトは「投資・資産運用が**学べるスクール**（教育サービス）の比較・紹介」という立て付けで構築してある。この立て付けを崩す変更を入れない。

- 断定的な利益表現・「必ず儲かる」「確実に増える」等は**禁止**
- リスク開示を**記事本文・開示バナー・footer・about・index に配置済み**。これらを削らない
  - 元本割れの可能性がある / 投資は自己責任 / 特定の投資商品を推奨しない / 成果は保証されない
- `scripts/generate.py` の `SYSTEM_PROMPT` は YMYL 投資向けに調整済み。**プロンプトを他サイトからコピーで上書きしない**
- 個別銘柄・具体的な投資判断・利回りの予測を書かない

---

## サイト固有情報

| 項目 | 内容 |
|---|---|
| ブランド | トウシナビ |
| 本番URL | https://aech22.github.io/invest-affiliate-blog/（**github.io サブパス配信・base 付き**） |
| 独自ドメイン | **未定**（後決め） |
| GitHub | `aech22/invest-affiliate-blog`（public） |
| 収益モデル | 成果報酬（A8.net） |
| 由来 | シカクナビのクローン（2026-08-04構築）。off-theme（投資/FX）を専用サイトに分離したもの |

## カテゴリ（2種・gender軸なし）

`stock`（株式投資スクール）/ `fx`（FX・外貨スクール）

## 承認済みサービス（`services.json`・`approved: true`）

| サービス | カテゴリ |
|---|---|
| ファイナンシャルアカデミー | stock |
| タケルFXスクール | fx |

比較記事2本。**テキストCTA（画像バナーなし）**なのでブラウザでスクショを撮ってよい。

## サイドバー

カテゴリ＋新着記事のみ（広告なし）。

## 固有のハマりどころ

- **base パス**: github.io サブパス配信のため `astro.config.mjs` に `base` 指定あり。内部リンクは全て `import.meta.env.BASE_URL` 方式。独自ドメイン移行時は `base` 行の削除だけ
- **サブエージェント作業後は必ず `git add -A && git commit` してから push**（シカクナビで初回デプロイがクローン元になった事故の再発防止）
- **新規 repo の workflow run は push 直後8秒では登録前のことがある**（少し待ってから確認する）

## 残タスク

- [ ] 独自ドメインの決定・取得
- [ ] `services.json` の seed 事実を一次情報で検証
- [ ] `public/ogp.png` 差し替え
